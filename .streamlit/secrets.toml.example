# Streamlit Community Cloud secrets template.
# In production, paste these into:  Dashboard → your app → Settings → Secrets
# (NOT into a file in the repo — secrets.toml is gitignored.)

SERPAPI_KEY = "your_serpapi_key_here"
